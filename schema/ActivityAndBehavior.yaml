biological process:                            # mapping
  represented_as: node              # schema configuration
  label_in_input: biolink:ActivityAndBehavior   # connection to input stream
  properties:
    name: str